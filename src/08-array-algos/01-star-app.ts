import { forward, left } from "introcs/turtle";

export let main = async () => {
    // TODO #1 - Call the star procedure
    
};

let star = (points: number, diameter: number, angle: number): void => {
    // TODO #0 - Star drawing algorithm!
    
};

main();